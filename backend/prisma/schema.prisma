// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

// Model for each message
model Message{
  id String @id @default(auto()) @map("_id") @db.ObjectId
  messageId String @db.ObjectId
  timestamp DateTime
  channelName Channel @relation(fields: [messageId], references: [id])
  author String
}

// Model For Channel
model Channel{
  id String @id @default(auto()) @map("_id") @db.ObjectId
  channelId  String @db.ObjectId
  serverName Server @relation(fields: [channelId], references: [id])
  messages Message[]
}

// Model For Server
model Server{
  id String  @id @default(auto()) @map("_id") @db.ObjectId
  Name String
  channels Channel[]
}